meta {
  name: Criar Pedido
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/orders
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "items": [
      {
        "productId": "{{productId}}",
        "quantity": 2
      }
    ]
  }
}
