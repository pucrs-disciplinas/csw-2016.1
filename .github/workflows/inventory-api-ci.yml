name: Inventory API CI

on:
  push:
    paths:
      - "inventory-api/**"
      - ".github/workflows/inventory-api-ci.yml"
  pull_request:
    paths:
      - "inventory-api/**"
      - ".github/workflows/inventory-api-ci.yml"

jobs:
  ci:
    name: Build, Lint, Test and Coverage
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: inventory-api

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: inventory-api/yarn.lock

      - name: Install dependencies
        run: |
          corepack enable
          yarn install --frozen-lockfile

      - name: Run build
        run: yarn build

      - name: Run lint
        run: yarn lint

      - name: Run unit tests
        run: yarn test

      - name: Generate coverage report
        run: yarn test:coverage

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        with:
          name: inventory-api-coverage
          path: inventory-api/coverage
          if-no-files-found: error

  release:
    name: Release to GitHub and npm
    runs-on: ubuntu-latest
    needs: ci
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    defaults:
      run:
        working-directory: inventory-api
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: inventory-api/yarn.lock
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: |
          corepack enable
          yarn install --frozen-lockfile

      - name: Run build
        run: yarn build

      - name: Get package version
        id: pkg
        run: echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: `inventory-api-v${{ steps.pkg.outputs.version }}`,
              name: `inventory-api v${{ steps.pkg.outputs.version }}`,
              generate_release_notes: true,
            });

      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}